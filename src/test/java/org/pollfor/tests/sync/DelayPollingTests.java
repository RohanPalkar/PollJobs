package org.pollfor.tests.sync;

public class DelayPollingTests {

    // hard delay is more than the provided timeout

    // hard delay is more than the default timeout

    // hard delay and it times out

    // hard delay and it succeeds

    // criteria delay and it times out

    // criteria delay and it succeeds

    // criteria delay with default timeout

    // criteria delay with input timeout

    // criteria delay when time interval is more than timeout



}
